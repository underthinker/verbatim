# Verbatim dogfood - tester report

Copy this file, fill it in, and send it back at the end of the two-week dogfood (M4 Phase E).
Everything here stays local until you choose to share it - Verbatim has no telemetry and sends nothing on its own.

## 1. Your setup

- OS and version (e.g. macOS 15.5 / Windows 11 23H2 / Ubuntu 24.04):
- Desktop (Linux only - GNOME Wayland / KDE Plasma / other):
- Machine (CPU, RAM, GPU if any):
- Install channel (Homebrew / winget / Flathub / AppImage / .dmg / .msi / .deb):

## 2. Onboarding

- Time from starting the installer download to your first successful dictation (target < 5 min, no docs):
- Did you need to read any docs or ask for help? If so, where did you get stuck:

## 3. Crash-free rate

Run `verbatim stats` in a terminal and paste the block here.
The `unclean shutdowns` count is the crash proxy; a normal quit does not add to it.

```
<paste `verbatim stats` output>
```

- Anything that crashed or hung that the counter might have missed:

## 4. Dictation quality (polish preference)

For as many dictations as you can (aim for ~10), paste the raw and polished text pair.
Do not label which you prefer - the blind panel scores that later.
Only submit pairs you are comfortable sharing.

| # | raw | polished |
|---|-----|----------|
| 1 |     |          |
| 2 |     |          |

## 5. Latency spot-checks

For a few ~10 s utterances, how did it feel, and (if shown) what latency did the app report?

- Raw transcription (target < 800 ms p50):
- With polish on (target < 1.5 s p50):

## 6. Bugs and friction

List anything that broke, confused, or annoyed you - one line each, roughest first.

-
-

## 7. Would you keep using it?

- Yes / No, and the one thing that most drives that answer:
